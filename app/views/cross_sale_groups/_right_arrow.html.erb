<%= link_to_remote image_tag('buttons/right-arrow.png', :style => "border: 0;"),
                   :url => edit_cross_sale_group_path(csg), :method => :get,
                   :html => {:href => url_for(edit_cross_sale_group_path(csg))} %>

